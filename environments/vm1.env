# Ad-hoc VM #1 — target for manually deploying an arbitrary branch (QA / demo).
# Adjust NUXT_PUBLIC_BASE_URL to the real host before use.
NUXT_PUBLIC_BASE_URL=https://vm1.nuxt-core-template.fivgpn.com

HOST=0.0.0.0
NITRO_PORT=3110

NUXT_PUBLIC_APP_DEBUG=1
NUXT_PUBLIC_APP_IS_PROD=

